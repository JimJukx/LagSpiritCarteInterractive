/* SUPPRIME TOUT LE CONTAINER (TWENTY TWENTY-FIVE) AUTOUR DE LA PAGE */
.page .wp-site-blocks,
.page .wp-block-group.alignwide,
.page .wp-block-group,
.page .entry-content,
.page .wp-block-post-content {
    margin: 0 !important;
    padding: 0 !important;
    max-width: 100% !important;
    width: 100% !important;
}

/* OPTIONNEL : enlever l'espèce de padding interne imposé */
.page main {
    padding: 0 !important;
    margin: 0 !important;
}
/* Faire en sorte que le bloc Lag Spirit prenne toute la hauteur de l'écran */
.lagspirit-wrapper {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
}

/* Le <main> doit grandir et contenir la carte + la sidebar sur toute la hauteur */
.lagspirit-wrapper main {
  flex: 1;
  display: flex;
  min-height: 0;
}

/* La carte doit s'étirer en hauteur */
.lagspirit-wrapper #map {
  flex: 2;
  height: 100%;
  min-height: 450px;
}
@media (max-width: 800px) {
  .lagspirit-wrapper #map {
    height: 50vh;      /* la carte prend seulement la moitié de l'écran */
    min-height: 50vh;
  }
}